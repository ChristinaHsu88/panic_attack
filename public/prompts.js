// DRY helper
function allPrompts(options){
  // pause game
  // pop up with options
  // skip time forward if YES
}

function promptEat(){
  // allPrompts(options)
  // increase energy if YES
}

function promptNap(){
  // allPrompts(options)
  // increase sleep if YES
}

function promptWorldEvents(){
  // allPrompts(options)
  // generate appropriate in-game response based on event options
}